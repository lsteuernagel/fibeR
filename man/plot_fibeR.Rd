% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{plot_fibeR}
\alias{plot_fibeR}
\title{Plot an object of class fibeR_data}
\usage{
plot_fibeR(
  fibeR_data,
  datatype = "raw",
  split_plots = TRUE,
  datatype_baseline = "decay",
  save_plot = FALSE,
  output_path = character(0),
  name_signal = "x465A",
  name_control = "x405A",
  linesize = 0.3,
  min_yaxis_dFF = 0.2,
  add_hline = TRUE,
  add_vline = TRUE,
  text_size_param = 15,
  color_vector = c(signal = "#009E73", control = "#D55E00", decay = "#0072B2", fit =
    "#CC79A7", median = "#E69F00")
)
}
\arguments{
\item{fibeR_data}{object of class fibeR_data}

\item{datatype}{character vector. 'raw' for raw data or one or multiple of 'decay','fit','median' for processed (dFF) data}

\item{split_plots}{logical. Whether to split ggplot by source (signal/control, dFFs). Defaults to TRUE.}

\item{datatype_baseline}{character vector. 'none' to ignore baseline in raw plot. One or multiple of 'decay','fit','median' for baseline that is used in processed data. If no valid baseline.data can be found, will automatically fall back to 'none'. Defaults to 'decay'.}

\item{save_plot}{logical. Whether to save ggplot object to output_path. Defaults to FALSE}

\item{output_path}{existing directory to save plot}

\item{name_signal}{character string with the name of the signal column in fibeR_input. Defaults to the MPI standard: 'x465A'}

\item{name_control}{character string with the name of the control column in fibeR_input. Defaults to the MPI standard: 'x405A'}

\item{linesize}{the ggplot line size. defaults to 0.3}

\item{min_yaxis_dFF}{for processed data only: Controls the minimum y axis value. If dFF is larger it will be automatically increased. Defaults to 0.2 (-20\% to 20\%).}

\item{add_hline}{for processed data only: logical. If TRUE a grey line is added at y=0}

\item{add_vline}{logical. If TRUE a grey line is added at y=intervention_time}

\item{text_size_param}{the ggplot text size. defaults to 15}

\item{color_vector}{a vector which maps custom colors to datatypes}
}
\value{
ggplot object
}
\description{
Plot the raw or processed data from a list-like fibeR_data using ggplot. The most important option is datatype ('raw' for raw data or 'decay','fit','median' for processed (dFF) data).
}
\details{
split_plots controls whether to split signal/control or the different dFFs into multiple facets.

When plotting processed data the function process_fibeR has to be run first!
}
